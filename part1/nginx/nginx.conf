worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    log_format main '$remote_addr - $remote_user [$time_local] '
                    '"$request" $status $body_bytes_sent '
                    '"$http_referer" "$http_user_agent" '
                    'request_id=$request_id';

    access_log /var/log/nginx/access.log main;

    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    sendfile on;
    keepalive_timeout 65;
    client_max_body_size 50M;

    # ============================================================
    # Rate Limiting Zones
    # ============================================================

    # Аутентификация: строгий лимит (brute-force защита)
    limit_req_zone $binary_remote_addr zone=login:10m rate=20r/m;

    # API-запросы (POST/PUT/PATCH/GET): стандартный лимит
    limit_req_zone $binary_remote_addr zone=write_api:10m rate=60r/m;

    # Генерация документов: тяжёлые операции
    limit_req_zone $binary_remote_addr zone=generation:10m rate=10r/m;

    # PMI Agent: самые долгие операции
    limit_req_zone $binary_remote_addr zone=pmi:10m rate=5r/m;

    # Общий лимит на весь API (последний рубеж)
    limit_req_zone $binary_remote_addr zone=global_api:10m rate=300r/m;

    # Возвращаем 429 вместо дефолтного 503 при превышении лимита
    limit_req_status 429;

    # Docker's embedded DNS — re-resolve backend hostnames every 30s.
    resolver 127.0.0.11 valid=30s ipv6=off;

    server {
        listen 80;
        server_name _;

        # Security headers
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options "DENY" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        # Pass X-Request-ID to all upstreams
        proxy_set_header X-Request-ID $request_id;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $host;

        # Return X-Request-ID to client
        add_header X-Request-ID $request_id always;

        # CORS headers
        add_header 'Access-Control-Allow-Origin' $http_origin always;
        add_header 'Access-Control-Allow-Credentials' 'true' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, PATCH, DELETE, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, X-Request-ID, X-Idempotency-Key' always;
        add_header 'Access-Control-Max-Age' 86400 always;

        # CORS preflight — headers already set above, just return 204
        if ($request_method = 'OPTIONS') {
            return 204;
        }

        # ============================================================
        # Block /internal/* from external access
        # ============================================================
        location /internal/ {
            return 403;
        }

        location /.well-known/ {
            return 403;
        }

        # ============================================================
        # Auth Service
        # ============================================================
        location /api/auth/ {
            limit_req zone=global_api burst=50 nodelay;
            limit_req zone=write_api burst=20 nodelay;

            set $auth_backend http://auth-service:8001;
            proxy_pass $auth_backend;
            proxy_read_timeout 30s;
        }

        # Login — самый строгий лимит (brute-force защита)
        location = /api/auth/login {
            limit_req zone=login burst=5 nodelay;
            set $auth_backend http://auth-service:8001;
            proxy_pass $auth_backend;
        }

        # Password reset — тоже строгий
        location = /api/auth/password/reset-request {
            limit_req zone=login burst=3 nodelay;
            set $auth_backend http://auth-service:8001;
            proxy_pass $auth_backend;
        }

        # ============================================================
        # Catalog Service
        # ============================================================
        location /api/catalog/ {
            limit_req zone=global_api burst=50 nodelay;
            limit_req zone=write_api burst=20 nodelay;

            set $catalog_backend http://catalog-service:8002;
            proxy_pass $catalog_backend;
            proxy_read_timeout 30s;
        }

        # ============================================================
        # Generation Service
        # ============================================================
        location /api/generation/ {
            limit_req zone=global_api burst=10 nodelay;
            limit_req zone=generation burst=3 nodelay;

            set $generation_backend http://generation-service:8003;
            proxy_pass $generation_backend;
            proxy_read_timeout 120s;  # генерация может занимать время
        }

        # ============================================================
        # Workflow Service
        # ============================================================
        location /api/workflow/ {
            limit_req zone=global_api burst=50 nodelay;
            limit_req zone=write_api burst=15 nodelay;

            set $workflow_backend http://workflow-service:8004;
            proxy_pass $workflow_backend;
            proxy_read_timeout 30s;
        }

        # ============================================================
        # PMI Agent  (/api/pmi/* → pmi-agent /api/v1/pmi/*)
        # ============================================================
        location /api/pmi/ {
            limit_req zone=global_api burst=5 nodelay;
            limit_req zone=pmi burst=2 nodelay;

            set $pmi_backend http://pmi-agent:8006;
            rewrite ^/api/pmi/(.*)$ /api/v1/pmi/$1 break;
            proxy_pass $pmi_backend;
            proxy_read_timeout 600s;  # PMI-прогон может занимать несколько минут
        }

        # ============================================================
        # Frontend (static files)
        # ============================================================
        location / {
            root /usr/share/nginx/html;
            try_files $uri $uri/ /index.html;
        }

        # Health check for nginx itself
        location = /health {
            return 200 '{"status":"ok","service":"nginx"}';
            add_header Content-Type application/json;
        }
    }

    # ============================================================
    # HTTPS server (uncomment for TLS)
    # ============================================================
    # server {
    #     listen 443 ssl;
    #     ssl_certificate /etc/nginx/certs/fullchain.pem;
    #     ssl_certificate_key /etc/nginx/certs/privkey.pem;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;
    #     add_header Strict-Transport-Security "max-age=31536000" always;
    #     # ... same location blocks as above
    # }
}
