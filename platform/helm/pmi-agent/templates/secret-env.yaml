{{- if .Values.secretEnv.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "pmi-agent.fullname" . }}-manual-env
  namespace: {{ .Values.namespaceOverride | default .Release.Namespace }}
  labels:
    {{- include "pmi-agent.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secretEnv.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
